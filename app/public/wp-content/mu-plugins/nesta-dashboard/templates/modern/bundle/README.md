Place `export.xml` and the zipped uploads archive generated from the Modern template kit in this directory.

- `export.xml` – WordPress WXR export that the installer imports.
- `uploads.zip` – Media archive extracted into the site’s uploads directory.

The Nesta dashboard will only show the “Install template” button once both files exist.
